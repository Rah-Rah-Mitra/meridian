# 1M ANN re-baseline — the P7 carry, closed (2026-06-13, Pi 5)

**Verdict: GATE PASS at full scale. The numkong rung-1 restoration holds at
1M vectors; the carry closes with numbers, and one knob re-derives.**
Release-bench artifact (CI run 27429009312, aarch64-gnu), suite 2 `ann` at
`--ann-vectors 1000000`, disk pre-flight 9G free (≥3GB transient bound
honored; scratch removed after).

| Metric | @1M (this run) | Gate / context |
|---|---|---|
| recall@10, ef=64 | 0.94 | the 100k-derived default ef no longer clears 0.95 at 1M |
| **recall@10, ef=128** | **0.98** | gate ≥0.95 ✓ — `derived_ef_search = 128` at Profile F |
| recall@10, ef=192 / 256 | 0.99 / 1.00 | headroom mapped |
| search p50 / p99, ef=128 | 0.78ms / **1.73ms** | gate p99 <40ms ✓ — 23× headroom |
| build RSS delta | 483MB | within the 520MB resident budget row |
| resident memory | 506MB | matches the budget row's extrapolated 506MB exactly |
| build throughput | 1890 vec/s | 1M build ≈ 8.8 min on-device |
| 16K-page mmap view smoke | pass | ADR-01 standing tripwire |

## What this closes and what it re-derives

- **Risk #3's standing rule honored at scale**: the suite-2 RECALL gate (not
  latency-only) re-ran at 1M on the device — the regime where the portable
  fallback's i8 cosine collapsed to recall 0.0 in the v0.2.0 incident. The
  numkong dispatch holds: 0.98 @ ef=128.
- **Knob re-derivation (budgets §3 note)**: `derived_ef_search` is
  scale-dependent — 64 at the 100k Profile R (recall 0.98 there), **128 at
  the 1M Profile F** (ef=64 decays to 0.94 at 1M). Profile F deployments
  should ship `vector.expansion_search = 128`; p99 cost of the bump is
  +0.7ms — noise against the 40ms gate.
- The Profile-F RAM math (506MB resident @1M) moves from "extrapolated ✓"
  to **measured ✓**.
