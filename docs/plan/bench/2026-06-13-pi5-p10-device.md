# Phase-10 device validation (2026-06-13, Pi 5)

Release-bench + deep binaries from CI run 27429009312 (post answer-mode
merge), run on the deployment host against the live searxng sidecar
(operator searx deadline 2500ms), per the P9-exit method.

## Standing suites (release-bench artifact)

fusion · synfarm · spike · evidence · ope · voi · changepoint · answer —
**8/8 gate PASS** (`voi-embed` ran from the local tree the same day —
RECORDED NO, see its own bench doc; the CI artifact predated its merge).

## 1M ANN re-baseline

See `2026-06-13-pi5-p10-ann-1m.md` — PASS at scale, P7 carry closed,
Profile-F `expansion_search` re-derived to 128.

## Deep p50 re-check + answer-mode budget row (n=16, interpolated medians)

| Arm | p50 | max | ok | Verdict |
|---|---|---|---|---|
| deep baseline (no fetch) | **2173ms** | 2898ms | 16/16 | ≤2.5s holds (P9: 2168ms) |
| answer mode (`fetch_budget=2&answer=true`) | **3224ms** | 5184ms | 16/16 | a-priori 3.0s row MISSED → **re-derived to ≤3.5s** (risk-#8 protocol) |

- **Two probe runs, both recorded.** Run 1 hit a degraded engine pool
  (deep baseline 2506ms lower-median of 8; several `searx_unavailable` —
  the documented suspension gotcha). Sidecar restart cleared it; run 2
  (above) is the measurement. Run 1's probe also used a lower-median
  statistic on n=8; run 2 uses interpolated medians on n=16.
- **Why answer mode costs ~+1.05s over deep at the median:** 9/16 requests
  ran the full budget (2 fetches, each realized by its own passage-CE batch
  — the mechanism that makes the Pandora stop honest and that suite 18
  validated). The 3.0s row was an a-priori guess from planning; the
  carried optimization study (per-doc passage count 16→8, re-validated
  through suite 18 before the knob moves) targets winning 3.0s back.
- **Answer-mode behavior on live traffic:** 9/16 delivered `best_passage`
  (raw CE logits, source URLs to arxiv.org pages the probe corpus
  surfaced); stop reasons observed: `budget_exhausted` ×9, `deadline` ×3,
  `exhausted` ×4 (no web candidates — engine flakiness); honesty markers
  exact: `answer_unavailable` whenever no passage materialized, compounding
  honestly with `rerank_timeout`/`searx_unavailable`.
